# Dependency directories
node_modules/
jspm_packages/

# Cache directories
.npm/
.npm-cache/
__pycache__/
.eslintcache
.parcel-cache
.turbo/

# Environment variables (security sensitive — never commit secrets)
# Backend .env contains DB credentials and JWT secret — never commit
backend/.env

# Frontend .env.local may contain local overrides — never commit
frontend/.env.local

# Operating System files
Thumbs.db
.DS_Store
